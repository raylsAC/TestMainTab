<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/common_bottom_tab_bg_selector"
    android:gravity="center_vertical" >

    <!-- tab内容，图片加文字 -->

    <ImageView
        android:id="@+id/main_tab_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:contentDescription="@string/app_name"
        android:gravity="center"
        android:src="@drawable/main_tab_index_selector" />

    <TextView
        android:id="@+id/main_tab_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/main_tab_img"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="3dp"
        android:gravity="center"
        android:text="@string/app_name"
        android:textAppearance="@style/TextAppearanceFrameTabText" />

    <View
        android:id="@+id/main_tab_tip"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_alignTop="@id/main_tab_img"
        android:layout_toRightOf="@id/main_tab_img"
        android:background="@mipmap/main_tab_tip_icon"
        android:visibility="gone" />

</RelativeLayout>